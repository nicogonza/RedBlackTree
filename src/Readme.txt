Added methods:
element class:
getkey()
getValue()
default constructor for redBlackNode
Validator has and array of size 50.
All feutures available and no extra added futures besides the assignment. The validator assumes the string is not empty.
RBT to string prints empty line after the tree. Each class has added helper methods.
License 1.0.
By Nicolas Gonzalez, applet pending.

Attached files:
all of the source code,Readme.txt and report.